AUTOLOAD_DEFAULT_DATA=true
# Optional: local data directory containing incidents.csv + reports.csv
# DEFAULT_DATA_DIR=/app/data
# Optional: remote snapshot archive URL (.tar.bz2). If set, takes precedence over DEFAULT_DATA_DIR.
# DEFAULT_SNAPSHOT_URL=https://example.com/backup-YYYYMMDDHHMMSS.tar.bz2
